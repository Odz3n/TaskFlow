

// using TaskFlow.Application.Features.Projects.Commands;

namespace TaskFlow.Application.Features.ValidationsTODO;

/*
public class AddMemberToProjectCommandValidator: AbstractValidator<AddMemberToProjectCommand>
{
    public AddMemberToProjectCommandValidator()
    {
        RuleFor(x => x.ProjectId)
            .NotEmpty().WithMessage("Project ID is required");

        RuleFor(x => x.UserId)
            .NotEmpty().WithMessage("User ID is required");

        RuleFor(x => x.ProjectRole)
            .IsInEnum().WithMessage("Please select a valid project role");
    }
}
*/